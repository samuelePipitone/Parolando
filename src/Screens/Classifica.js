import React from "react";
import { View, Text } from "react-native";

export default function Classifica({ navigation }){
	return(
		<View style={{flex: 1}}>
			<Text>IMPOSTAZIONI PORCODDIO</Text>
		</View>
	);
}